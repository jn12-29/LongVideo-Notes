You are creating a chapter outline for notes from a Chinese lecture.
Use adjacent content blocks to form natural chapters for readers. Do not make every block its own chapter. Merge adjacent blocks when they are part of the same argument, example, or transition.
For short lectures or a small number of blocks, prefer fewer coherent chapters over many tiny chapters. Chapter titles should be concise lecture-note section names without numbering.
Write every chapter title and summary in Simplified Chinese. Keep English technical terms, proper nouns, abbreviations, formulas, code identifiers, and original slide terms when preserving the original wording is clearer. Do not translate established English terms mechanically.

Output format: return JSON matching {"chapters":[{"id":1,"title":"...","summary":"...","block_id_start":0,"block_id_end":0}]}.
Target chapter count hint: {{ target_chapter_count_hint }}. Treat it as guidance, not a hard requirement.

Hard rules for ids and ranges:
- Chapter id is 1-based: use 1, 2, 3, ... in order.
- Block ids are 0-based consecutive ids from 0 to the last input block id below.
- The first chapter must have block_id_start = 0.
- Each chapter must satisfy block_id_start <= block_id_end.
- Each next chapter must have block_id_start equal to the previous chapter's block_id_end + 1.
- The last chapter must have block_id_end equal to the last input block id shown below.
- Cover every input block exactly once.
- Do not skip, duplicate, overlap, or reorder block ids.
- Use only block ids in the consecutive range from 0 to the last input block id shown below.

Before returning, check the chapter ids and block ranges yourself.

Blocks:
{% for block in blocks -%}
- {{ block.id }}: {{ block.topic }} — {{ block.summary }}
{% endfor %}
