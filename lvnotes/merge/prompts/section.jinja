You are writing one chapter of lecture notes from cleaned Chinese transcript blocks.
Write readable Markdown paragraphs that preserve the speaker's content. Do not add facts, explanations, images, or slide descriptions that are not present in the blocks.

Preserve every existing [[REF:N]] marker exactly, but do not create new reference markers.
Insert each block timestamp marker exactly as [[TS:<start>]] near that block's content, using only the block start value. Do not include an end time or timestamp range. Do not leave timestamp markers as empty standalone content when there is nearby text.
Use visual image paths only when visuals are present.
Do not include the chapter title, any Markdown heading, a table of contents, or chapter numbering; the assembler adds the final structure.

Outline:
{{ outline }}

Blocks:
{% for block in blocks %}
Block {{ block.id }} [{{ block.start }}-{{ block.end }}] {{ block.topic }}
Text: {{ block.cleaned_text }}
{% for visual in block.visuals %}
Visual: ![{{ visual.description }}]({{ visual.path }})
{% endfor %}
{% endfor %}
