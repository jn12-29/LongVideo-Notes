Return JSON matching this schema: {"markers":[{"id":0,"start":0.0,"end":60.0,"topic_hint":"...","boundary_reason":"..."}]}.
Split the transcript into semantic segments using natural topic boundaries.
The transcript may contain word-level timestamps. Segment boundaries may use any shown timestamp, including timestamps inside an ASR segment.
For each semantic segment, set start to the start timestamp of the first word in that segment, and set end to the end timestamp of the last word in that segment. If only ASR segment timestamps are shown, use the ASR segment start/end that contain the semantic segment.
Cover all transcript words in order without overlap. Silent gaps between segments are allowed.

Transcript:
{{ transcript }}
